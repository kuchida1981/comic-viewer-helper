## 1. テストの準備

- [ ] 1.1 `src/logic.test.js` に、第1ページ（index 0）が常に見開きペアから除外されることを検証するテストケースを追加する
- [ ] 1.2 `src/logic.test.js` に、最終ページが常に見開きペアから除外されることを検証するテストケースを追加する
- [ ] 1.3 `src/logic.test.js` に、2ページのみの構成時に両方のページが単一表示されることを検証するテストケースを追加する

## 2. 実装

- [ ] 2.1 `src/logic.js` の `fitImagesToViewport` 関数内、ペアリング判定ループを修正する
- [ ] 2.2 インデックス 0 の画像について `pairWithNext` を強制的に false にするロジックを追加する
- [ ] 2.3 次の画像が最終ページ（`i + 1 === allImages.length - 1`）の場合に `pairWithNext` を強制的に false にするロジックを追加する

## 3. 検証

- [ ] 3.1 ユニットテストを実行し、すべてのテストがパスすることを確認する
- [ ] 3.2 ビルドが正常に完了することを確認する
