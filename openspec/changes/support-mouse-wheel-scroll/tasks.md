## 1. 準備

- [x] 1.1 `src/logic.js` にホイールイベントの判定ロジック（deltaY の閾値、方向判定）を追加する
- [x] 1.2 スロットル処理（クールダウン）のためのユーティリティ関数を `src/logic.js` に追加、または `src/main.js` での実装方針を確定する

## 2. コア実装

- [x] 2.1 `src/main.js` に `wheel` イベントリスナーを追加し、`isEnabled` の状態に応じて動作するように実装する
- [x] 2.2 ホイールの回転方向と現在の表示モード（単一/見開き）を考慮して、適切なインデックスで `jumpToPage` を呼び出す
- [x] 2.3 クールダウン期間（500ms）とスクロール閾値（50px）を適用し、意図しない連続ジャンプを抑制する

## 3. 検証と調整

- [x] 3.1 `src/logic.test.js` に追加した判定ロジックのユニットテストを追加する
- [x] 3.2 実際のブラウザ環境での動作確認（単一ページ/見開き表示、高速スクロール時の挙動）
- [x] 3.3 ビルド、リンターの実行、および `npx openspec validate --all` によるバリデーション確認

## 4. 修正: 中途半端なスクロール位置の防止

- [ ] 4.1 `design.md` および `specs` を更新し、標準スクロールの完全な抑制を明記する
- [ ] 4.2 閾値未満のホイール操作でも `preventDefault()` を実行し、位置ずれを防ぐ
- [ ] 4.3 再度テストとビルドを行い、動作を確認する
