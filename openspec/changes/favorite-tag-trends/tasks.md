## 1. データ構造と保存ロジックの拡張

- [ ] 1.1 `src/types.ts` の `RelatedWork` インタフェースに `tags?: Tag[]` を追加する
- [ ] 1.2 `src/managers/UIManager.ts` の `toggleFavorite` メソッドを更新し、お気に入り追加時にメタデータからタグを保存するようにする
- [ ] 1.3 `src/store.test.ts` または適切なテストで、タグを含むお気に入りデータの永続化を検証する

## 2. トレンド集計ロジックの実装

- [ ] 2.1 `src/ui/components/FavoritesModal.ts` 内にタグの出現頻度を集計する `calculateTrends` 関数を実装する
- [ ] 2.2 集計結果を出現回数の降順でソートし、上位10件を抽出するロジックを実装する
- [ ] 2.3 集計ロジックのユニットテストを作成し、既存の（タグなし）データとの混在時も正しく動作することを確認する

## 3. UI コンポーネントの追加と統合

- [ ] 3.1 `src/ui/components/FavoritesModal.ts` にトレンド表示用のセクションを作成する `createTrendSection` 関数を実装する
- [ ] 3.2 トレンドセクションをお気に入り一覧のタイトルの下に挿入し、スタイルを適用する
- [ ] 3.3 タグボタンのクリックイベントを実装し、`DiscoveryManager` を介して検索を実行するように統合する
- [ ] 3.4 お気に入り削除時にトレンド表示が即座に更新されることを確認する

## 4. 最終確認とバリデーション

- [ ] 4.1 `npm run test` を実行し、既存のテストがパスすることを確認する
- [ ] 4.2 `npm run build` を実行し、ビルドエラーがないことを確認する
- [ ] 4.3 `npx openspec validate` を実行し、仕様との整合性を確認する
