## 1. ロジックと定数

- [ ] 1.1 GUI位置保存用のストレージキーと定数を定義する。

## 2. キーボードナビゲーション

- [ ] 2.1 `onKeyDown` を更新し、`j` (次へ) と `k` (前へ) キーで `scrollToImage` を呼び出すようにする。

## 3. ドラッグ可能なGUIの実装

- [ ] 3.1 ビューポートの境界チェックと、位置の保存・読み込みを行うヘルパー関数を実装する。
- [ ] 3.2 `createNavigationUI` を更新し、ドラッグ開始のための `mousedown` リスナーを追加する。
- [ ] 3.3 `mousemove` と `mouseup` ハンドラを実装し、パネル位置 (`top`/`left`) を更新する。
- [ ] 3.4 パネルに `cursor: move` スタイルを適用し、ドラッグ中のテキスト選択を防ぐために `userSelect: none` を設定する。
- [ ] 3.5 ロード時にストレージからパネル位置を初期化し、未設定または範囲外の場合はデフォルト位置に戻す。