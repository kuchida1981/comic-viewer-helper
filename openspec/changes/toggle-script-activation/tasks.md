## 1. ロジックの実装 (logic.js)

- [ ] 1.1 `revertToOriginal()` 関数を実装し、インラインスタイルとDOM構造（ラッパーの除去）をリセットする
- [ ] 1.2 `revertToOriginal()` のユニットテストを追加する

## 2. UIの実装 (main.js)

- [ ] 2.1 ON/OFF を切り替えるためのトグルボタンをナビゲーションパネルに追加する
- [ ] 2.2 状態 (`isEnabled`) に応じてパネル全体を隠し、最小化アイコンを表示するロジックを実装する
- [ ] 2.3 最小化状態でもドラッグによる位置変更ができるようにする

## 3. 機能の連携

- [ ] 3.1 `onKeyDown` や `updatePageCounter` で `isEnabled` をチェックするように修正する
- [ ] 3.2 状態変更時に `localStorage` への保存と、スタイルの再適用/リセットを即座に実行する
- [ ] 3.3 初期化プロセスで `isEnabled` を読み込み、初期状態を制御する
