## 1. 開発環境のクリーン化

- [x] 1.1 `.gitignore` に `dist/` を追加し、ビルド成果物を除外対象とする
- [x] 1.2 `git rm -r --cached dist/` を実行し、既存のビルド成果物を Git の追跡から外す
- [x] 1.3 既存の `dist/` ディレクトリを物理的に削除し、クリーンな状態にする

## 2. 自動デプロイフローの構築

- [x] 2.1 配布専用ブランチ（例: `release`）をローカルまたはリモートで作成する
- [x] 2.2 `.github/workflows/deploy.yml` を新規作成し、`main` ブランチへのプッシュをトリガーとしたビルド＆デプロイ処理を記述する
- [x] 2.3 デプロイ用のアクション（`JamesIves/github-pages-deploy-action` 等）を設定し、ビルド成果物を `release` ブランチにプッシュするように構成する

## 3. インストール URL の更新と検証

- [x] 3.1 `README.md` および `README.ja.md` 内のインストール用リンクを `release` ブランチの Raw URL に書き換える
- [ ] 3.2 実際に `main` ブランチへプッシュ（またはテスト実行）し、`release` ブランチが正しく更新されることを確認する
- [ ] 3.3 新しい Raw URL からのインストールが正常に機能することを検証する
