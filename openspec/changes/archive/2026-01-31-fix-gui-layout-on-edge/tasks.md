## 1. 実装

- [x] 1.1 `src/main.js` の `createNavigationUI` 関数内で、GUIコンテナのスタイル定義に `white-space: nowrap` を追加する。
- [x] 1.2 `src/main.js` の `createNavigationUI` 関数内で、GUIコンテナのスタイル定義に `flex-wrap: nowrap` を追加する。
- [x] 1.3 `src/main.js` の `createNavigationUI` 関数内で、GUIコンテナのスタイル定義に `width: max-content` を追加する。

## 2. 検証

- [x] 2.1 ビルドを実行し、Userscriptを生成する (`npm run build`)。
- [x] 2.2 ブラウザで動作確認を行い、GUIパネルを画面端にドラッグしてもレイアウトが崩れないことを確認する。
- [x] 2.3 ウィンドウサイズを変更してもGUIパネルが縮小されないことを確認する。
