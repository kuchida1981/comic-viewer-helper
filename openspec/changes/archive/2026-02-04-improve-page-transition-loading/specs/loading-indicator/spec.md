## ADDED Requirements

### Requirement: ローディングインジケーターの表示
システムは、画像の読み込み待機や遷移処理によりユーザー操作がブロックまたは遅延する場合、視覚的なインジケーターを表示しなければならない（SHALL）。

#### Scenario: 待機中の表示
- **WHEN** ユーザーが次ページへの遷移操作を行い、遷移先の画像がまだ読み込まれていないとき
- **THEN** 画面中央にローディングインジケーターが表示される
- **AND** 画像の読み込みが完了し遷移が実行されると、インジケーターは非表示になる
