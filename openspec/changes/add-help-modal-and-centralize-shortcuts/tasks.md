## 1. 定義の一元管理

- [x] 1.1 `src/shortcuts.js` を新規作成し、既存のキーボードショートカットの定義を抽出する
- [x] 1.2 `src/main.js` で `src/shortcuts.js` をインポートし、`onKeyDown` ハンドラを新しい定義形式に対応させる（リファクタリング）

## 2. ヘルプモーダルの実装

- [x] 2.1 `src/ui/components/HelpModal.js` を新規作成する
- [x] 2.2 `HelpModal.js` にて `src/shortcuts.js` を参照し、ショートカット一覧を表示するUIを実装する
- [x] 2.3 `src/ui/styles.js` にヘルプモーダル用のスタイル（必要に応じて）を追加する

## 3. アプリケーションとの統合

- [x] 3.1 `src/store.js` に `isHelpModalOpen` 状態を追加する
- [x] 3.2 `src/main.js` にてヘルプモーダルの表示/非表示トグルロジック（`?` キーおよびGUIボタン用）を実装する
- [x] 3.3 `src/ui/components/NavigationButtons.js` に `?` ボタンを追加する
- [x] 3.4 モーダル表示中のキーガード（Esc以外のキー入力を無効化）を実装する

## 4. 動作確認とテスト

- [x] 4.1 `?` キーでヘルプモーダルが表示/非表示されることを確認する
- [x] 4.2 ナビゲーションパネルの `?` ボタンでヘルプモーダルが表示されることを確認する
- [x] 4.3 `Esc` キーまたはオーバーレイクリックでヘルプモーダルが閉じることを確認する
- [x] 4.4 新しいユニットテスト（`src/ui/components/components.test.js` など）を追加し、モーダルの生成を確認する

## 5. 動作確認フィードバックの修正

- [x] 5.1 ヘルプ表示中に `?` キーでモーダルを閉じられるように修正する（トグル動作の完成）
- [x] 5.2 ヘルプ表示中にマウスホイールによるスクロールが発生しないようにガードを追加する
