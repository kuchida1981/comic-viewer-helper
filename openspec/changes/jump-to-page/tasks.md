## 1. ロジックの実装

- [x] 1.1 `src/logic.js` に、インデックスから画像要素を取得する関数、またはスクロール位置を特定する関数を追加する
- [x] 1.2 `src/logic.test.js` に新しい関数のユニットテストを追加し、パスすることを確認する

## 2. UIの更新

- [x] 2.1 `src/main.js` の `createNavigationUI` を修正し、ページ表示部分を `<input type="number">` に置き換える
- [x] 2.2 `input` 要素のスタイルを調整し、パネルのデザインに馴染ませる

## 3. インタラクションの実装

- [x] 3.1 `input` 要素に `keydown` リスナーを追加し、`Enter` キーでジャンプを実行するようにする
- [x] 3.2 入力値のバリデーションロジック（1未満、または総数超えの場合の処理）を実装する
- [x] 3.3 ジャンプ実行後に `input.blur()` を呼び出し、フォーカスを外す処理を追加する

## 4. 仕上げと検証

- [x] 4.1 スクロール時に `input` 内の数値が現在のページ番号と同期し続けることを確認する
- [x] 4.2 ビルド（`npm run build`）を実行し、`.user.js` が正しく生成されることを確認する