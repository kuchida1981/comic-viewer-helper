## 1. 実装

- [ ] 1.1 `src/main.ts` の `App.init()` メソッドを修正し、`this.popUnderBlocker.init()` を `this.adapter.getContainer()` の前に移動する。

## 2. 検証

- [ ] 2.1 ビューアーが起動するページ（作品詳細ページで画像がある場合）で `PopUnderBlocker` が正常に動作することを再確認する。
- [ ] 2.2 ビューアーが起動しないページ（画像がない作品詳細ページや検索結果ページ）で `PopUnderBlocker` が初期化され、クリックをインターセプトすることを確認する。
- [ ] 2.3 `npm run test` を実行し、既存のテストがパスすることを確認する。
- [ ] 2.4 新しい仕様（ビューアー未起動ページでの動作）をカバーするテストケースを追加し、パスすることを確認する。
